const continents = require('./continents')
const languages = require('./languages')
const countries = require('./countries')

const supporteddLanguages = require('./supported-languages')

module.exports = {
    continents,
    languages,
    countries,
    supporteddLanguages
}