const continentQuery = require('./continentQuery.midd')
const countriesQuery = require('./countriesQuery.midd')
const language = require('./language.midd')
const languageQuery = require('./languageQuery.midd')
const listByLanguage = require('./listByLanguage.midd')

module.exports = {
    continentQuery,
    countriesQuery,
    language,
    languageQuery,
    listByLanguage
}