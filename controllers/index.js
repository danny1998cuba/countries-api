const continentController = require('./continents.controller')
const countriesController = require('./countries.controller')
const languagesController = require('./languages.controller')
const mail = require('./mail.controller')

module.exports = {
    continentController,
    countriesController,
    languagesController,
    mail
}